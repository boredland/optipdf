Um Scan-Tailor nicht zu benutzen habe ich folgenden Vorschlag (noch ungetestet).

#Step 1 | Vorbereitung | Convertiert mit Ghostscript zu hochauflösenden PGM
gs -sDEVICE=pgmraw -dNOPAUSE -dBATCH -r600 -sOutputFile=./foo1/foo-%04d.pgm foo.pdf

#Step 2 | Seiten teilen, drehen, schwarze Ränder entfernen | Nutzt das Programm "unpaper" (http://unpaper.berlios.de/)
##In der Webanwendung sollte man den Benutzer die Variable "--layout" setzen lassen. "double" für doppelseitigen Input, "single" für einseitigen
mkdir foo2
cd foo1
/usr/local/bin/parallel -v unpaper --layout double -op 2 {} ../foo2/{.}-%02d.pgm -- *.pgm

#Step 4 | Text in pgm erkennen
echo tessedit_create_hocr 1 > config_file
/usr/local/bin/parallel -v tesseract {} {.} -l deu config_file -- *.pgm

#Step 4 | In Pdf zusammenführen (hocr2pdf)

